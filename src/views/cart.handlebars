<h1>Cart</h1>
{{#each products}}
  <div>
    <h3>{{this.name}}</h3>
    <p>{{this.description}}</p>
    <p>Price: ${{this.price}}</p>
    <p>Quantity: {{this.quantity}}</p>
  </div>
{{/each}}
<p>Total: ${{cart.total}}</p>